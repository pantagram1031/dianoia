# Result

Headline: Corpus theorem `t2ivt` directly proves the intermediate value theorem with endpoint-inclusive opposite-sign hypotheses.

The proof is replayed from the self-contained corpus entry. The statement matches the original target, including the cases f(a)=0 or f(b)=0.
