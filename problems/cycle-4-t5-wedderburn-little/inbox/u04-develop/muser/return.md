# Muser Return

Spark 1: Convert the normalizer quotient gap into an arithmetic lemma about q^n-1 factors before returning to division rings.
Spark 2: Try a minimal n argument: if n is the least maximal-subfield degree causing failure, proper subdegrees should be unavailable.
Spark 3: Ask the number-theorist to isolate exactly where the standard proof gets its divisibility contradiction, then re-prove that lemma standalone.