# Next Session

Resume the RESEARCH_CONTRIBUTION objective.

Immediate next step:
Begin P11 ATTEMPTS. P10 curation is complete with 20 `OPEN-VERIFIED`
candidates across 10 areas in `research-bank/INDEX.md`.

Recommended first attempt:
`research-bank/R020/` no-three-in-line problem, rank A.

First concrete deliverable:
Create a P11 attempt log for `R020` and build a replayable verifier for one
known `D(n)=2n` certificate from the 2026 CSP frontier before attempting any
new search claim. Record the attempt in `research-bank/R020/log.md` with one
of: `SOLVED-CLAIM`, `PARTIAL-PROGRESS`, `OBSTRUCTION-FOUND`, or
`NO-PROGRESS`.

Current state:
- Active goal: RESEARCH_CONTRIBUTION. Do not mark complete unless a real
  contribution passes the configured gates or an honest ceiling condition is
  reached.
- Prior MASTERPIECE baseline remains historical evidence, not the active stop
  condition.
- P9 INFRA is complete: enhanced `connectors/arxiv/`, `connectors/lean/`,
  openness/novelty skills, templates, and research-state verifier exist.
- P10 CURATION is complete: 20 `OPEN-VERIFIED` candidates across 10 areas plus
  `R001` as an `OPEN-WEAK` warm-up candidate.
- P11 ATTEMPTS is ready. Prefer rank A/B candidates with exact-checkable
  artifacts: `R020`, `R013`, `R017`, `R005`, `R008`, `R009`, `R012`, `R015`,
  `R016`, `R019`, or `R021`.
- P12 verification remains mandatory for any `SOLVED-CLAIM`: adversarial
  novelty check, five Reviewer D attacks, computational/formal verification
  where applicable, and a `CLAIMS.md` row with confidence.

Operational reminders:
- Run `git fetch; git pull origin main --rebase` first.
- Read `ROADMAP.md`, `DEVLOG.md`, `RESEARCH_LOG.md`, and this file before
  choosing work.
- Commit and push every logical unit.
- Run `python tools\verify_all.py` before final status; historical B1-B5 token
  accounting warnings are expected unless the verifier changes.
