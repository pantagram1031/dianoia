# R013 Boundary Recurrence Notes

date: 2026-05-21
status: OBSERVATION-NOT-CLAIM

## Scope

This note interprets the extracted P7/P8 boundary cases in:

```text
normal-form-cases/vector-boundary/
```

The cases are generated by:

```text
python tools/poset_balance.py matrix-vector-named-cases research-bank/R013/attempt-20260520/width3-rank2221-u6-dangerous-form-ledger.json --ids P7,P8 --output-dir research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary --summary research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary/summary.json
```

The two actual `13/32` boundary forms are:

```text
p7-form-2: pair (b,c), first/second split 19/13 out of 32.
p8-form-1: pair (c,e), first/second split 19/13 out of 32.
```

Reports and depth-2 recurrence traces are:

```text
p7-form-2-report.json
p7-form-2-recurrence-depth2.json
p8-form-1-report.json
p8-form-1-recurrence-depth2.json
```

## Recurrence Comparison

At depth 1, P7 form 2 decomposes as:

```text
choose a: 11 first, 13 second
choose b:  8 first,  0 second
total:    19 first, 13 second
```

At depth 1, P8 form 1 decomposes as:

```text
choose a: 12 first,  9 second
choose b:  7 first,  4 second
total:    19 first, 13 second
```

Both boundary forms therefore reach the same `19/13` split, but the imbalance
is distributed differently across initial choices.

## Interpretation

This suggests the P7/P8 boundary may not require two unrelated hand proofs.
The common target is a `19` versus `13` extension count. A useful next proof
move is to search for a branch-level transformation that moves four extensions
between the first two branches while preserving the total split.

The two boundary forms differ by shifting one adjacent cover between the
bottom and middle layers. In vector language, P7 has adjacent delta `(0,1,0)`
from U6 while P8 has `(1,0,0)`, and both share skip delta `(1,0,1)`.

## Next Proof Move

Attempt a paired recurrence proof:

1. Write the depth-2 leaves of `p7-form-2` and `p8-form-1` side by side.
2. Identify common subtraces after deleting the shifted adjacent edge.
3. Try to construct an injection from the 13 minority extensions into the 19
   majority extensions that works for both forms.

If the paired recurrence proof fails, keep P7/P8 as two separate boundary
lemmas and continue extracting recurrence ledgers for P1/P2/P3.

## Claim Discipline

No `CLAIMS.md` row. This is a recurrence comparison inside a restricted finite
subcase, not a proof of the published open problem.
