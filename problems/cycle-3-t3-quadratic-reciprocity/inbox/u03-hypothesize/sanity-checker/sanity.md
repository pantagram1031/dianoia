# Sanity Checker

H1 accepted for development. Checks: exponent equality `((p-1)(q-1))/4 = ((p-1)/2)((q-1)/2)` for odd primes; no boundary point on `iq=jp`; no assumption that p<q; no use of quadratic reciprocity as an input.