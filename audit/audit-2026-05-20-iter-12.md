# dianoia Continuous Improvement Audit

Date: 2026-05-20
Audited commit: `489f9675a83c25b79cbc5a78d3fce3e98fdfb2f0`
Mode: read-only source audit; no dianoia phase prompt was invoked.

## SECTION 1. File inventory

Layer 1 anchors are present: `IDENTITY.md`, `goal.md`, `AGENTS.md`.
Phase prompts are present: `prove.md`, `00-intake.md` through `06-consolidate.md`, `checkpoint.md`, `halt.md`, `resume.md`, and utility prompts.
Top-level route wrappers now exist for `muse` and `consult`.
Subagents are present: muser, prover, researcher, reviewer, sanity-checker, skill-author, specialist-factory, surveyor.
Specialist registry and seed profiles are present.
Corpus index and `corpus/theorems/.gitkeep` are present.
Templates now include `context_manifest.md`, both session state templates, proof/review/subproof templates, and problem skeleton placeholders including `notes/formalization/.gitkeep`.
`CHANGELOG.md` records iter-1 through iter-11.

## SECTION 2. Layer coherence trace

The constitutional layer is intact and was not modified during immediate tasks.
The phase layer now has explicit `[PRECONDITIONS]` in every phase prompt from intake through consolidation.
The subagent layer is wired into natural phases except for researcher, which is still only reachable indirectly from specialist-factory and resume.
The specialist layer supports consult mode through `prompts/consult.md`; panel etiquette exists but no top-level panel prompt or route exists.
The corpus layer now has a theorem destination directory.
The template layer now supports context manifests and MSP narration fields.

## SECTION 3. Phase loop trace

Fresh invocation still routes `AGENTS.md` default input to `prompts/prove.md`.
`prove.md` creates a problem skeleton, initializes state, and runs the phase loop with Phase 5 after every non-review phase.
`05-review.md` now accepts a reviewed phase label or infers it from artifact path, so the loop can select phase-dependent review context.
Review after Phase 0 no longer requires `survey.md`.
Reviewer D fires for intake, hypothesize, develop, and consolidate reviews.
Each phase still invokes checkpoint, and checkpoint now writes `context_manifest.md`.
Halt and resume are coherent for v4 closed versus resume-eligible states.

## SECTION 4. v4 integration check

The v4 direct-attack, persistence, reviewer D, meaningfulness, and resume guard invariants are integrated.
B1 through B2, M1 through M7, and m1 through m2 from the prior audit have source-level patches.
Open v4 tension: `needs_human` is mentioned in state and checkpoint, but no explicit non-evasion decision rule tells phases when it is legitimate to ask the user versus keep attacking, researching, or recording a gap.

## SECTION 5. Cross-reference check

Resolved: `AGENTS.md -> prompts/muse.md`, `AGENTS.md -> prompts/consult.md`, `05-review.md -> reviewer D`, `06-consolidate.md -> corpus/theorems/`, `goal.md -> templates/context_manifest.md`.
Resolved subagent wiring: `01-survey -> surveyor`, `03-hypothesize -> sanity-checker and muser`, `04-develop -> prover and muser`, `06-consolidate -> skill-author`.
Weak edge: `researcher.md` is not explicitly invoked by `01-survey.md` for four-field citation verification.
Weak edge: panel mode is described in specialist etiquette but has no prompt-level entry point.

## SECTION 6. Subagent coverage

`prover.md`: invoked by Phase 4 for separable lemma delegation.
`surveyor.md`: invoked by Phase 1 for bounded literature branches.
`researcher.md`: indirectly reachable from specialist-factory and resume only; not yet part of ordinary survey citation verification.
`reviewer.md`: invoked by Phase 5 for A/B/C and gated D.
`sanity-checker.md`: invoked by Phase 3 for each hypothesis.
`muser.md`: reachable from `prompts/muse.md`, Phase 3, and Phase 4 stuck-state handling.
`specialist-factory.md`: reachable from Phase 2 and resume.
`skill-author.md`: reachable from Phase 6 for reusable techniques.

## SECTION 7. Specialist registry consistency

`specialists/INDEX.md` has analyst, geometer, and combinatorialist rows with matching profile files.
Support files exist for each seed specialist.
Consult mode can address a registered specialist by slug.
No source rule currently starts dialogue automatically when two specialists disagree, and panel mode has etiquette but no prompt.

## SECTION 8. Corpus integrity

`corpus/INDEX.md` remains empty except for schema rows.
`corpus/theorems/` exists with `.gitkeep`.
No corpus theorem rows are present, so no sha or self-containment checks can be performed.
Phase 6 now has a valid destination for future self-contained WN entries.

## SECTION 9. Template completeness and behavior scores

Templates now cover claim ledger, proof, review, subproof request, context manifest, session state, problem skeleton, and formalization notes.
The problem skeleton has directories for proofs, failures, reviews, inbox, ideas, dialogue, archive, notes, and notes/formalization.

Eight behavior scores:
1. Proving: 4/5. WN proof discipline, prover delegation, and formalization notes exist; no formalization checkpoint rule yet.
2. Verifying: 4/5. A/B/C/D review is wired with phase-dependent inputs; no sample review fixture exists.
3. Attempting: 4/5. Direct attack and stuck-state persistence are explicit; repeated-session attack novelty is reviewed by D.
4. Ideating: 3/5. Muser fires when stuck and top-level muse exists; specialist sparks and cross-specialist idea flow are still weak.
5. Asking: 2/5. `needs_human` exists but lacks a source-level decision gate separating genuine blockage from evasion.
6. Researching collaboratively: 3/5. Consult mode exists and dialogue directory exists; panel mode and disagreement-triggered dialogue are not reachable.
7. Organizing materials: 4/5. Ledger, journal, context manifest, corpus destination, and skeleton placeholders are coherent.
8. Reading papers: 3/5. Survey and surveyor require four-field citations; researcher verification is not part of the normal survey path.

## SECTION 10. Discrepancies and recommendations

MAJOR DIM-5: `needs_human` lacks a decision rule. Recommendation: add a checkpoint-level gate requiring an exhaustion record, exact user question, and non-evasion exclusions before setting `needs_human.flag=true`.

MAJOR DIM-6: Panel mode is not reachable from a top-level prompt or AGENTS route. Recommendation: add `prompts/panel.md` or an explicit consult extension for multi-specialist disagreement.

MAJOR DIM-8: Researcher is not part of ordinary survey citation verification. Recommendation: invoke `prompts/subagents/researcher.md` from `01-survey.md` for unresolved or high-load references.

MINOR DIM-1: Formalization notes exist, but no checkpoint or Phase 4 rule records when a Lean/Coq stub was attempted or intentionally skipped.

Priority: patch MAJOR DIM-5 first because it ties for severity and has the lowest current behavior score.
