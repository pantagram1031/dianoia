# Hypotheses Live

## H1 Direct Attack
For all a < b and continuous f : [a,b] -> R with opposite-sign endpoint condition, there exists c in [a,b] with f(c)=0.
Refutation condition: supremum cut construction fails to force f(c)=0.
Sanity check: reduce to f(a) <= 0 <= f(b); define S={x in [a,b] : f(x) <= 0}; c=sup S; continuity rules out f(c)<0 and f(c)>0.
Status: survives.

## Survivors
H1 direct attack selected.
