# R020 Raw Attempt

date: 2026-05-20
mode: direct one-pass attempt, no dianoia verifier or phase loop
verdict: NO-PROGRESS

## Problem

Make progress on the no-three-in-line problem by either finding a new
`D(n)=2n` certificate, improving a bound, or producing an independently
checkable obstruction.

## Attempt

The direct attempt identified the 2026 Prellberg CSP frontier as the most
promising source of exact certificates. It did not produce a new construction
or proof. Without a dedicated verifier, the raw route could only cite the
published table and paper abstract rather than independently replay the
certificate.

## Outcome

`NO-PROGRESS` toward a new mathematical result. The raw path found a source but
did not create a reusable verification artifact, did not search beyond the
published frontier, and did not produce a new claim.

## Evidence

- Source located: Thomas Prellberg, "Constraint Satisfaction Programming for
  the No-three-in-line Problem", arXiv:2602.07751.
- Relevant published claim: configurations of `2n` points for all `n <= 60`;
  first unknown case is `n = 61`.
- No independent replay artifact produced in the raw path.
