# R013 Boundary Mechanism Notes

date: 2026-05-21
status: OBSERVATION-NOT-CLAIM

## Scope

This note promotes the depth-3 P7/P8 boundary observations into a reusable
mechanism vocabulary, generated by:

```text
python tools/poset_balance.py recurrence-mechanism-summary \
  research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary/p7-form-2-recurrence-depth3.json \
  --output research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary/p7-form-2-mechanism-depth3.json

python tools/poset_balance.py recurrence-mechanism-summary \
  research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary/p8-form-1-recurrence-depth3.json \
  --output research-bank/R013/attempt-20260520/normal-form-cases/vector-boundary/p8-form-1-mechanism-depth3.json
```

The mechanism summary is intended as proof-search infrastructure: it classifies
recurrence leaves by forced orientation blocks and balanced residual cores. It
does not encode a special P7/P8 theorem.

## P7 Form 2

Artifact:
`normal-form-cases/vector-boundary/p7-form-2-mechanism-depth3.json`.

P7 form 2 is a `forced-block` mechanism. The pair is `(b,c)`, with root split
`19/13` out of `32`.

The depth-3 leaves are:

```text
forced first total:  19
forced second total: 13
unseen total:         0
split counts:         5:0 x2, 3:0 x3, 0:5 x1, 0:4 x2
```

Proof target:
show directly that the eight depth-3 initial strings partition the linear
extensions, then certify the terminal counts `5,3,3,5,3` versus `5,4,4` by
small chain/shuffle lemmas.

## P8 Form 1

Artifact:
`normal-form-cases/vector-boundary/p8-form-1-mechanism-depth3.json`.

P8 form 1 is a `balanced-core-plus-forced-first` mechanism. The pair is
`(c,e)`, with root split `19/13` out of `32`.

The depth-3 leaves are:

```text
forced first total:  6
forced second total: 0
unseen first total:  13
unseen second total: 13
unseen total:        26
split counts:        3:0 x2, 4:4 x3, 1:1 x1
```

Proof target:
show that the two `3:0` leaves are forced-first blocks and that each unseen
leaf is self-balanced by a local involution or equal chain/shuffle count.

## Why This Is General

The important abstraction is not "P7 and P8 pass." It is:

```text
recurrence tree -> forced blocks + balanced cores -> local proof obligations
```

That abstraction can be reused on P1/P2/P3 and on future finite combinatorial
attempts. It helps dianoia choose proof tactics from structure:

- use forced-block enumeration when no unseen residual remains;
- use balanced-core involutions when unseen residuals split evenly;
- avoid claiming a global bijection when the recurrence mechanisms differ.

## Claim Discipline

No `CLAIMS.md` row. This remains evidence for a restricted width-3,
height-4, rank-layer-shape `2,2,2,1` finite subcase, not a proof of the
published open problem.
