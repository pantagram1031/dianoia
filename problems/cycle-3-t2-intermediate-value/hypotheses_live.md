# Hypotheses Live

## H1 Direct Attack
For all real a < b and continuous f : [a,b] -> R with opposite-sign endpoint condition, there exists c in [a,b] with f(c)=0.
Sanity check: corpus theorem `t2ivt` has the same endpoint-inclusive quantifiers.
Status: survives.
